export const REQUIRED = 'This field is required';
export const PASSOWRD_VALIDATION =
    'At least 8 characters, max 32 characters,  one uppercase letter, one lowercase letter, one number and one special case character';
export const CONFIRM_PASSWORD_MATCH = 'Confirm password does not match with password';
export const PASSWORD_REGEXP = /^(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&])[A-Za-z\d@$!%*?&]{8,32}$/;